#!/usr/bin/env bash
# Wrapper: delegate to scripts/build_package.sh
exec "$(dirname "$0")/scripts/build_package.sh" "$@"
#!/usr/bin/env bash
# Wrapper: delegate to scripts/build_package.sh
exec "$(dirname "$0")/scripts/build_package.sh" "$@"
