#!/usr/bin/env bash
# Wrapper: delegate to scripts/create_v1.0.3_release.sh
exec "$(dirname "$0")/scripts/create_v1.0.3_release.sh" "$@"
#!/usr/bin/env bash
# Wrapper: delegate to scripts/create_v1.0.3_release.sh
exec "$(dirname "$0")/scripts/create_v1.0.3_release.sh" "$@"
