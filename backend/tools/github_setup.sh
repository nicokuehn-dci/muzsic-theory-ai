#!/usr/bin/env bash
# Wrapper: delegate to scripts/github_setup.sh
exec "$(dirname "$0")/scripts/github_setup.sh" "$@"
#!/usr/bin/env bash
# Wrapper: delegate to scripts/github_setup.sh
exec "$(dirname "$0")/scripts/github_setup.sh" "$@"
