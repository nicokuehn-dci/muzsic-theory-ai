#!/usr/bin/env bash
# Wrapper: delegate to scripts/scheduled_push.sh
exec "$(dirname "$0")/scripts/scheduled_push.sh" "$@"
#!/usr/bin/env bash
# Wrapper: delegate to scripts/scheduled_push.sh
exec "$(dirname "$0")/scripts/scheduled_push.sh" "$@"
