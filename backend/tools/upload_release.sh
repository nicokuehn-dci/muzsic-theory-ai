#!/usr/bin/env bash
# Wrapper: delegate to scripts/upload_release.sh
exec "$(dirname "$0")/scripts/upload_release.sh" "$@"
#!/usr/bin/env bash
# Wrapper: delegate to scripts/upload_release.sh
exec "$(dirname "$0")/scripts/upload_release.sh" "$@"
VERSION="1.0.0"
