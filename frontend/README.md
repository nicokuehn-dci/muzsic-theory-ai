# Frontend (React)

This folder contains a minimal React frontend scaffold (Vite). To run the frontend:

```bash
cd frontend
npm install
npm run dev
```

The frontend is independent from the existing Python backend and is intended as a starting point.
