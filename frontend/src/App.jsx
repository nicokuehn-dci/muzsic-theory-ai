import React from 'react'
import Header from './components/Header'

export default function App() {
  return (
    <div className="app">
      <Header />
      <main>
        <h2>Welcome to Music Theory AI (Frontend)</h2>
        <p>This is a minimal React scaffold. Connect to a backend API or extend the UI from here.</p>
      </main>
    </div>
  )
}
